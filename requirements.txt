# Framework principal
Django>=4.2,<5.0
djangorestframework>=3.14,<4.0  # Opcional, API futura (Fase 3)
django-cors-headers>=4.0,<5.0

# Base de datos (PostgreSQL)
psycopg2-binary>=2.9,<3.0

# Autenticación y seguridad
argon2-cffi>=23.0,<24.0
django-ratelimit>=4.0,<5.0

# Utilidades
python-decouple>=3.8,<4.0  # Variables de entorno
Pillow>=10.0,<11.0          # Imágenes (subidas de evidencia)
reportlab>=4.0,<5.0         # Generación de PDFs (exportación)

# Testing
pytest>=7.4,<8.0
pytest-django>=4.7,<5.0
pytest-cov>=4.1,<5.0
factory-boy>=3.3,<4.0

# Desarrollo
ipython>=8.0,<9.0
django-debug-toolbar>=4.2,<5.0  # Solo desarrollo
